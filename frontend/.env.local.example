# Copy this file to .env.local and adjust if your Spring gateway runs somewhere else.
# Next.js only exposes env vars prefixed with NEXT_PUBLIC_ to the browser.
NEXT_PUBLIC_GATEWAY_URL=http://127.0.0.1:8080
